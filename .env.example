# The base URL of your site.
# For local development, this is typically http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- GitHub Content Management ---
# Your GitHub username or organization name that owns the content repository.
GITHUB_OWNER=""

# The name of the repository where your content JSON files are stored (e.g., "iscream-content").
GITHUB_REPO=""

# A GitHub Personal Access Token with "repo" scope to allow reading and writing content.
GITHUB_TOKEN=""

# The branch to read content from (defaults to "main" if not set).
GITHUB_BRANCH="main"
