import { SITE } from "@/lib/site";

export default function EventsPage() {
  const src = `https://calendar.google.com/calendar/embed?src=${encodeURIComponent(SITE.email)}&ctz=America%2FDenver`;
  return (
    <div className="space-y-4">
      <h1 className="text-3xl font-bold">Events</h1>
      <p className="text-sm text-zinc-600">Updates appear instantly when edited in Google Calendar.</p>
      <div className="rounded-2xl border overflow-hidden">
        <iframe src={src} style={{ border: 0 }} width="100%" height={600} frameBorder={0} scrolling="no" />
      </div>
    </div>
  );
}
